# Scripts arquivados

Ficheiros `patch_*.py` foram usados uma vez para aplicar alterações quando o código já estava noutro branch; a lógica correspondente vive em `app/` e `frontend/`. Não é necessário executá-los num checkout atual.
